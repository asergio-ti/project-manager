import baseSchema from './base-schema.json';
import documentMapping from './document-mapping.json';
import documentMappingInstance from './document-mapping-instance.json';

export const coreSchemas = {
    base: baseSchema,
    documentMapping: documentMapping,
    documentMappingInstance: documentMappingInstance
};

export default coreSchemas; 